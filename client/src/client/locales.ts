/** `dshflow` namespace dictionaries (view tab label). */

/** Dictionary namespace owned by this plugin. */
export const NS = 'dshflow'

/** Simplified Chinese dictionary. */
export const zh = {
  'view.dshflow': '插件',
} as const

/** English dictionary. */
export const en = {
  'view.dshflow': 'Plugins',
} as const
